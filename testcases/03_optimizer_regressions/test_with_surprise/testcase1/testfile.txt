int main() {
    int a = getint(), b = getint();
    for (;getint();) {
        int temp = a;
        a = b;
        b = temp;
		// If remove PHI, pay attention to the sequence of MOVE here
		printf("%d %d %d", a, b, temp);
    }
    return 0;
}