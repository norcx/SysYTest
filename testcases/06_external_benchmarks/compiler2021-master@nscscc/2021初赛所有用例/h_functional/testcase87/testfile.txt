int gcd(int m, int n)
{
    if (n == 0) {
        return m;
    }
    return gcd(n, m % n);
}

int main()
{
    int k = getint();
    for(; k > 0;) {
        int a = getint();
        int b = getint();
        printf("%d", gcd(a, b));
        printf("\n");
        k = k - 1;
    }
    return 0;
}
